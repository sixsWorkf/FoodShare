<view class="show-info">
  <view class="right">
    <van-tabs  active="{{ active }}" bind:change="onChange">
      <block  wx:for="{{dishindexs}}" wx:for-item="dishindex">
      <van-tab  title="{{dishindex.name}}">
          <scroll-view style="height:{{0.865*imageheight}}rpx" scroll-y >
  <!--我已点菜-->
  <view class="mydishes">
    <view class="box">
    <van-collapse value="{{ activeNames }}" bind:change="onChange1">
     <block wx:for="{{dish}}" wx:for-item="dish" >
     <block wx:if="{{(dish.dishindexs==dishindex.name)||dishindex.name==indextrue}}"><!-- 索引只显示自己的判断!-->
                  <view style="height:{{0.13*imageheight}}rpx" class="list_item"> 
                    <view class="mes_box">
                      <view class="other_title text-xl text-content">{{dish.name}}</view>
                      <view class="other_detail">
                          <text class="text-l text-content1">点菜人：{{dish.peoplename}}</text>

                        <text class="text-xxl text-content2 text-red">￥{{dish.price}}</text>  
                      </view>

                    </view>
                    <view class="delete_box">
                      <text class="text-xl text-white">删</text>
                      <text class="text-xl text-white">除</text>
                    </view>
                  </view>   

       

        

       




     </block>
     </block>
    </van-collapse>
    </view>
  </view>
  </scroll-view>
    </van-tab>
      </block>
  </van-tabs>
  <view class="pay_box">
    <view class="all text-xxl text-content3 text-white"><text>合计：</text></view>
    <view class="price text-xxxl text-white text-content3"><text>￥{{all_price}}</text></view>
    <view class="pay">
      <text class="pay_font text-white">付款·></text>
    </view>
  </view>
  </view>
</view>
